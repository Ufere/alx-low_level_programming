#include "main.h"
/**
 * add - adds two integers and return their result
 *
 * @x: number being added
 *
 * @y: number being added
 *
 * Return: Always 0.
 *
 */
int add(int x, int y)
{
	return (x + y);
}
